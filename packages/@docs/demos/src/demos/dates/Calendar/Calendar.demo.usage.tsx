import { Calendar } from '@nex-ui/dates';
import { MantineDemo } from '@nex-uix/demo';

const code = `
import { Calendar } from '@nex-ui/dates';

function Demo() {
  return <Calendar />;
}
`;

function Demo() {
  return <Calendar />;
}

export const usage: MantineDemo = {
  type: 'code',
  centered: true,
  component: Demo,
  code,
};
